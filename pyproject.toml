[project]
name = "mirage-ops"
version = "2.0.0"
description = "Development and validation tooling for Mirage infrastructure"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "ansible-core==2.19.7",
  "yamllint==1.37.1",
]

[tool.uv]
package = false
